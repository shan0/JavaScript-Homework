function replaceWhiteSpace(input) {
    var replaceRegEx = input.replace(/\s/g, "");
    return replaceRegEx;
}

console.log(replaceWhiteSpace("But you were living in another world tryin' to get your message through"));